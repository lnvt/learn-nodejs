<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button class="btn btn-info">SUBCRIBE</button>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <img width="30%" src="{{course.image}}" alt="">
            <h3>{{course.description}}</h3>
        </div>
    </div>
</div>